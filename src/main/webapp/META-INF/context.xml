<?xml version="1.0" encoding="UTF-8"?>
<Context>

    <!-- Configuración de multipart para Tomcat 11+ (despliegue externo) -->
    <!-- El límite de partes se configura mediante System.setProperty en TomcatConfiguration.java -->

    <!-- Permitir parsing multipart casual (necesario para algunos filtros) -->
    <JarScanner scanClassPath="true"/>

</Context>